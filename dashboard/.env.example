# Somnia Network Configuration
# RPC URL for Somnia Dream testnet (Chain ID: 50312)
SOMNIA_RPC_URL=https://dream-rpc.somnia.network

# Somnia Stream Writer contract address (obtained from setupSomnia.ts script)
# Run: cd .. && npx tsx scripts/setupSomnia.ts
SOMNIA_STREAM_ADDRESS=0xYourSomniaStreamWriterAddress

# Schema ID for the price metrics data structure
SOMNIA_SCHEMA_ID=0xYourSchemaIdFromSetupScript

# Deployed MetricsUpdater contract address
# Obtained from: forge script script/Deploy.s.sol --rpc-url $SOMNIA_RPC_URL --broadcast
METRICS_UPDATER_ADDRESS=0xYourDeployedMetricsUpdaterAddress

# Telegram bot URL for price alerts (optional)
NEXT_PUBLIC_TELEGRAM_BOT_URL=https://t.me/your_bot_name

# Dashboard auto-refresh interval in milliseconds (default: 60 seconds)
# Recommended: 60000 (1 minute) to check for updates every minute
# The contract updates every 10 minutes via Chainlink Automation
NEXT_PUBLIC_REFRESH_INTERVAL_MS=60000

# Configure tracked pairs via shared/pairs.js
# Make sure the Chainlink price feed addresses are valid for the Somnia network
